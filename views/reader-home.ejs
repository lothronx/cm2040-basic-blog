<!------------------------- START OF MY CODE ------------------------->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <title><%= blog.title %></title>
  </head>
  <body class="font-mono">
    <!--the link to the Author - Home Page -->
    <header class="relative">
      <a
        href="<%= link %>"
        class="absolute top-10 right-10 border border-green-700 bg-white text-green-700 rounded-md px-4 py-2 m-2 transition duration-500 ease select-none hover:bg-green-50 focus:outline-none focus:shadow-outline"
        >ADMIN</a
      >
    </header>

    <!-- main part -->
    <main class="md:mx-40">
      <!-- the blog information -->
      <section class="grid justify-items-center pt-16">
        <h1 class="text-3xl font-bold text-green-900"><%= blog.title %></h1>
        <p class="text-lg pt-2"><%= blog.author %></p>
        <p class="pt-2"><%= blog.subtitle %></p>
      </section>

      <!-- the list of published articles -->
      <section class="pt-10">
        <ul class="gird grid-cols-1">
          <% articles.forEach((article) => { %>
          <li class="p-5 border-b border-dashed border-b-gray-500">
            <!-- each article includes title and publication date -->
            <article>
              <a href="articles/<%= article.id %>">
                <h2 class="text-lg text-green-900"><%= article.title %></h2>
                <time class="text-gray-500 text-sm"><%= article.published_at%></time>
              </a>
            </article>
          </li>
          <% }) %>
        </ul>
      </section>
    </main>
  </body>
</html>

<!------------------------- END OF MY CODE ------------------------->
